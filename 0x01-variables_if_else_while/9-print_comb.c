#include <stdio.h>

/**
 * main - Entry file
 *
 * Return: Always (0) Success
 */

int main(void)
{
	int i;
	for (i = 1 ; j < 10; j++)
	{
		putchar(i + '0');
		if (i != 9)
		{
			putchar(',');
			putchar(' ');
		}
	}
	return (0);
}
